# 3Dモデル管理ガイド

## 📋 概要

このガイドでは、ConsoleのPreviewPanelで3Dモデル（GLBファイル）を挿入・変更する方法を説明します。

## 🎯 3Dモデルの挿入方法

### ステップ1: 商品データベースページにアクセス

1. Consoleにログイン
2. サイドバーから「データベース」→「商品」を選択
3. 商品一覧が表示されます

### ステップ2: アセット管理ダイアログを開く

1. 商品一覧から、3Dモデルを追加したい商品の行を見つける
2. その行の「アセット管理」ボタンをクリック
3. 「アセット管理」ダイアログが開きます

### ステップ3: 3Dモデル（GLBファイル）を追加

1. **サイズを選択**
   - ドロップダウンから「S」「M」「L」のいずれかを選択
   - 各サイズごとに異なる3Dモデルを設定できます

2. **GLBファイルのURLを入力**
   - 「GLB URL」フィールドに、3DモデルファイルのURLを入力
   - 例: `https://example.com/models/jacket-s.glb`
   - または、ローカルファイルの場合: `/3d/model_men.glb`（publicフォルダ内）

3. **追加ボタンをクリック**
   - 「追加」ボタンをクリックしてアセットを保存

### ステップ4: プレビューで確認

1. 商品一覧で「プレビュー」ボタンをクリック
2. 右側のプレビューパネルが開きます
3. 電話フレーム内に3Dモデルが表示されます
4. サイズを変更すると、対応する3Dモデルが表示されます

## 🔄 3Dモデルの変更方法

### 方法1: 既存アセットを更新

1. 「アセット管理」ダイアログを開く
2. 変更したいサイズのアセットを見つける
3. 新しいGLB URLを入力して追加
   - **注意**: 同じサイズで新しいアセットを追加すると、自動的にバージョンが増加します
   - 最新バージョンがプレビューで使用されます

### 方法2: バージョン管理

- 同じサイズで複数のアセットを追加すると、自動的にバージョンが管理されます
- 例: Mサイズで3つのアセットを追加すると、バージョン1、2、3が作成されます
- プレビューでは、常に最新バージョン（最大のバージョン番号）が使用されます

## 📁 GLBファイルの配置場所

### ローカルファイルの場合

GLBファイルを`apps/console/public/3d/`フォルダに配置すると、以下のURLでアクセスできます：

```
/3d/model_men.glb
/3d/model_women.glb
/3d/jacket-s.glb
```

### 外部URLの場合

CDNやクラウドストレージにアップロードしたGLBファイルのURLを直接指定できます：

```
https://cdn.example.com/models/jacket-s.glb
https://storage.googleapis.com/bucket/models/jacket-m.glb
```

## 🎨 サイズごとのモデル設定

各サイズ（S/M/L）ごとに異なる3Dモデルを設定できます：

- **Sサイズ**: 小さいサイズ用のモデル（例: `jacket-s.glb`）
- **Mサイズ**: 中間サイズ用のモデル（例: `jacket-m.glb`）
- **Lサイズ**: 大きいサイズ用のモデル（例: `jacket-l.glb`）

プレビューでサイズを変更すると、対応する3Dモデルが自動的に切り替わります。

## 🔧 技術的な詳細

### アセットのデータ構造

```typescript
{
  id: string;
  productId: string;
  size: "S" | "M" | "L";
  glbUrl: string;
  version: number; // 自動生成
  createdAt: string;
  updatedAt: string;
}
```

### APIエンドポイント

- **GET** `/api/assets?productId={productId}` - 商品のアセット一覧を取得
- **POST** `/api/assets` - 新しいアセットを追加

### プレビューパネルの実装

- ConsoleのPreviewPanelは、`@atelier/preview`パッケージのVanilla JS実装を使用
- 電話フレーム内に埋め込まれ、3Dモデルと身長スライダーを表示
- Widgetのモーダルと同じデザイン・機能を提供

## ❓ よくある質問

### Q: GLBファイルはどこにアップロードすればいいですか？

A: 以下の方法があります：
1. **ローカル**: `apps/console/public/3d/`フォルダに配置
2. **外部**: CDNやクラウドストレージにアップロードしてURLを指定

### Q: 同じサイズで複数のモデルを追加するとどうなりますか？

A: 自動的にバージョンが管理され、最新バージョンがプレビューで使用されます。

### Q: モデルが表示されない場合は？

A: 以下を確認してください：
1. GLB URLが正しいか
2. ファイルがアクセス可能か（CORS設定など）
3. ブラウザのコンソールでエラーを確認

### Q: 身長スライダーは何のためにありますか？

A: 3Dモデルの表示サイズを調整するためのものです。実際のユーザーの身長に合わせてモデルを表示できます。

## 📝 まとめ

1. **商品データベース** → **アセット管理** → **GLB URLを入力** → **追加**
2. **プレビュー**ボタンで確認
3. サイズごとに異なるモデルを設定可能
4. バージョン管理は自動

これで、ConsoleのPreviewPanelで3Dモデルを管理できます！
